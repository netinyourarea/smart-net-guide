# Smart Net Guide

Client-side single-page app: plain Vite + `@tanstack/react-router` (file-based
routing), React 19, Tailwind v4. No server rendering — `src/main.tsx` mounts
the router into `index.html`'s `#root`.
